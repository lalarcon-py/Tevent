services:
  - type: web
    name: guild-manager
    runtime: node
    plan: free
    buildCommand: npm install && cd frontend && npm run build
    startCommand: node backend/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000