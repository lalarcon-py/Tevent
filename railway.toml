[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install"

[deploy]
startCommand = "cd backend && npx sequelize-cli db:migrate --force && node index.js"
restartPolicyType = "ON_FAILURE"

[mount]
source = "backend"
destination = "/app"

[service]
name = "TVGM"