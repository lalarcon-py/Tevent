import { Box, Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Guild Dashboard</Typography>
      {/* Add dashboard content here */}
    </Box>
  );
};

export default Dashboard;